import React from "react";

const FunnelChart = ({ data }) => (
  <div className="funnel-chart">
    {/* Aquí iría la lógica/renderizado de la gráfica de embudo */}
    <span>Gráfica de embudo</span>
  </div>
);

export default FunnelChart;
