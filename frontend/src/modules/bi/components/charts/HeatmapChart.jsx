import React from "react";

const HeatmapChart = ({ data }) => (
  <div className="heatmap-chart">
    {/* Aquí iría la lógica/renderizado de la gráfica de heatmap */}
    <span>Gráfica de heatmap</span>
  </div>
);

export default HeatmapChart;
