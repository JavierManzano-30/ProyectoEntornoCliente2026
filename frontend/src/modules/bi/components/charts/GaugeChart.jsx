import React from "react";

const GaugeChart = ({ data }) => (
  <div className="gauge-chart">
    {/* Aquí iría la lógica/renderizado de la gráfica de gauge */}
    <span>Gráfica de gauge</span>
  </div>
);

export default GaugeChart;
