import React from "react";

const AreaChart = ({ data }) => (
  <div className="area-chart">
    {/* Aquí iría la lógica/renderizado de la gráfica de área */}
    <span>Gráfica de área</span>
  </div>
);

export default AreaChart;
