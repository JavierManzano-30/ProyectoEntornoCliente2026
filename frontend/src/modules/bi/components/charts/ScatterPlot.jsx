import React from "react";

const ScatterPlot = ({ data }) => (
  <div className="scatter-plot">
    {/* Aquí iría la lógica/renderizado de la gráfica de dispersión */}
    <span>Gráfica de dispersión</span>
  </div>
);

export default ScatterPlot;
