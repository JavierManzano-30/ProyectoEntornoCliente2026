import React from "react";

const DonutChart = ({ data }) => (
  <div className="donut-chart">
    {/* Aquí iría la lógica/renderizado de la gráfica de dona */}
    <span>Gráfica de dona</span>
  </div>
);

export default DonutChart;
