import React from "react";

const PieChart = ({ data }) => (
  <div className="pie-chart">
    {/* Aquí iría la lógica/renderizado de la gráfica de pastel */}
    <span>Gráfica de pastel</span>
  </div>
);

export default PieChart;
