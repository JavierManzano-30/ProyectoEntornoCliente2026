import React from "react";

const BarChart = ({ data }) => (
  <div className="bar-chart">
    {/* Aquí iría la lógica/renderizado de la gráfica de barras */}
    <span>Gráfica de barras</span>
  </div>
);

export default BarChart;
