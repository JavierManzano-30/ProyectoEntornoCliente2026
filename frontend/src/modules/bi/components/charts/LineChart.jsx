import React from "react";

const LineChart = ({ data }) => (
  <div className="line-chart">
    {/* Aquí iría la lógica/renderizado de la gráfica de líneas */}
    <span>Gráfica de líneas</span>
  </div>
);

export default LineChart;
