import React from "react";

const WidgetContainer = () => (
  <div className="widget-container">
    {/* Aquí se renderizarán los widgets de BI (gráficos, tablas, etc.) */}
  </div>
);

export default WidgetContainer;
